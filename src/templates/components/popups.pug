.popup#popup
    .popup__body
        .popup__content
            button.popup__close.close-popup(type="button")
                img(src="./assets/images/close-popup.svg")
            h2.popup__title Запросить график аренды
            p.popup__text Заолните поля ниже и мы свяжемся с Вами в ближайшее время.
            form.popup__form(action="#")
                input.popup__form-input#name(type="text" placeholder="Имя" required pattern="^[а-яА-ЯёЁa-zA-Z0-9]+$")
                input.popup__form-input#phone-number(type="tel" placeholder="Телефон" required pattern="\+7\s?[\(]{0,1}9[0-9]{2}[\)]{0,1}\s?\d{3}[-]{0,1}\d{2}[-]{0,1}\d{2}")
                input.popup__form-input#email(type="email" placeholder="E-mail" required pattern="[^@\s]+@[^@\s]+")
                .popup__agreement
                    button.popup-link.popup__form-button(type="submit") Отправить
                    p.popup__agreement-text Отправляя данную форму, Вы соглашаетесь на обработку персональных данных
